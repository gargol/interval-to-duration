Interval to duration converter
----
This is a simple converter for MySQL intervals formatted as `${VALUE} ${DURATION}` (e.g.: `1 YEAR`, `6 months`) to moment.js [duration](http://momentjs.com/docs/#/durations/) value
